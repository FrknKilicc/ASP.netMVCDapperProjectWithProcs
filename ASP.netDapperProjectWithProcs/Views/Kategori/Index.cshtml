@model IEnumerable<ASP.netDapperProjectWithProcs.Models.Kategoriler>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.KategoriAdi)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.KategoriAdi)
        </td>
        <td>
            @Html.ActionLink("Yeni Ekle Güncelle", "AddorUpKategori", new {id=item.KategoriID}, new { @class = "btn btn-success" }) |
            @Html.ActionLink("Detay", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Sil", "DeleteKategori", new { id = item.KategoriID  },new {@class="btn btn-warning"})
        </td>
    </tr>
}

</table>
